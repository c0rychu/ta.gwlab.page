# Build output — everything here is regenerated by `make`
dist/

# Downloaded toolchain (tailwind standalone binary)
.tools/

# Generated PDFs never live in the repo (force one in with `git add -f` if ever needed)
*.pdf

# LaTeX build junk
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc

# OS / editor
.DS_Store
